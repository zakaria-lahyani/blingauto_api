# =============================================================================
# DEVELOPMENT CONFIGURATION TEMPLATE
# Perfect for development and testing - no SMTP setup required!
# =============================================================================

# Email Provider Configuration (Mock - no real emails sent)
AUTH_EMAIL_PROVIDER=mock

# Email Branding (for email templates)
AUTH_FROM_EMAIL=noreply@localhost
AUTH_SUPPORT_EMAIL=support@localhost
AUTH_APP_NAME=Car Wash Service (Dev)
AUTH_APP_URL=http://localhost:8001

# Development Database (use environment variables in docker-compose)
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}

# JWT Settings (use environment variable)
AUTH_JWT_SECRET_KEY=${AUTH_JWT_SECRET_KEY}

# Optional: Email Settings
AUTH_EMAIL_VERIFICATION_EXPIRE_HOURS=24
AUTH_EMAIL_VERIFICATION_RATE_LIMIT_MINUTES=1  # Faster for testing
AUTH_PASSWORD_RESET_EXPIRE_HOURS=2
AUTH_PASSWORD_RESET_RATE_LIMIT_HOURS=1

# Development Settings
GLOBAL_ENVIRONMENT=development
GLOBAL_DEBUG=true
LOGGING_LOG_LEVEL=DEBUG

# =============================================================================
# MOCK EMAIL PROVIDER:
# - Logs emails to console instead of sending them
# - Perfect for development and testing
# - No SMTP configuration required
# - See email content in application logs
# =============================================================================