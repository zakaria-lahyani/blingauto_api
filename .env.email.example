# =============================================================================
# CAR WASH API - EMAIL CONFIGURATION EXAMPLE
# Copy these settings to your .env file and configure for your environment
# =============================================================================

# Email Configuration
# Set to 'smtp' for production, 'mock' for development/testing
AUTH_EMAIL_PROVIDER=smtp
AUTH_SMTP_HOST=smtp.gmail.com
AUTH_SMTP_PORT=587
AUTH_SMTP_USERNAME=your-email@gmail.com
AUTH_SMTP_PASSWORD=your-app-password
AUTH_SMTP_USE_TLS=true
AUTH_FROM_EMAIL=noreply@yourcompany.com
AUTH_SUPPORT_EMAIL=support@yourcompany.com
AUTH_APP_NAME=Car Wash Service
AUTH_APP_URL=https://yourcompany.com

# Email Verification Settings
AUTH_EMAIL_VERIFICATION_EXPIRE_HOURS=24
AUTH_EMAIL_VERIFICATION_RATE_LIMIT_MINUTES=5

# Password Reset Settings
AUTH_PASSWORD_RESET_EXPIRE_HOURS=2
AUTH_PASSWORD_RESET_RATE_LIMIT_HOURS=1

# =============================================================================
# EMAIL PROVIDER SPECIFIC CONFIGURATIONS
# =============================================================================

# Gmail Configuration (most common)
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Generate an App Password: https://support.google.com/accounts/answer/185833
# 3. Use the App Password as AUTH_SMTP_PASSWORD (not your Gmail password)
# AUTH_SMTP_HOST=smtp.gmail.com
# AUTH_SMTP_PORT=587
# AUTH_SMTP_USE_TLS=true

# SendGrid Configuration
# 1. Sign up for SendGrid account
# 2. Create an API key
# 3. Use API key as password with username 'apikey'
# AUTH_SMTP_HOST=smtp.sendgrid.net
# AUTH_SMTP_PORT=587
# AUTH_SMTP_USERNAME=apikey
# AUTH_SMTP_PASSWORD=your-sendgrid-api-key
# AUTH_SMTP_USE_TLS=true

# AWS SES Configuration
# 1. Set up AWS SES and get SMTP credentials
# 2. Verify your sending domain/email
# AUTH_SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# AUTH_SMTP_PORT=587
# AUTH_SMTP_USERNAME=your-ses-smtp-username
# AUTH_SMTP_PASSWORD=your-ses-smtp-password
# AUTH_SMTP_USE_TLS=true

# Outlook/Hotmail Configuration
# AUTH_SMTP_HOST=smtp-mail.outlook.com
# AUTH_SMTP_PORT=587
# AUTH_SMTP_USE_TLS=true

# Custom SMTP Configuration
# AUTH_SMTP_HOST=mail.yourcompany.com
# AUTH_SMTP_PORT=587
# AUTH_SMTP_USE_TLS=true