# =============================================================================
# PRODUCTION REQUIREMENTS - CAR WASH API
# Core dependencies for production deployment
# =============================================================================

# Core FastAPI and async support
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database
sqlalchemy[asyncio]>=2.0.23
asyncpg>=0.29.0  # PostgreSQL async driver
aiosqlite>=0.19.0  # SQLite async driver (for testing/dev)

# Authentication and security  
python-jose[cryptography]>=3.3.0  # JWT tokens with cryptography support
bcrypt>=4.0.1,<4.2.0  # Password hashing - compatible version
passlib[bcrypt]>=1.7.4  # Password utilities

# Configuration and environment
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
email-validator>=2.1.0

# Logging and monitoring
structlog>=23.2.0

# Email functionality
aiofiles>=23.2.0  # For template loading
jinja2>=3.1.4  # Email templating

# Distributed caching and rate limiting
redis[hiredis]>=5.0.0  # Redis client with high-performance parser

# AWS Secrets Manager (optional)
boto3>=1.34.0  # AWS SDK (optional, for secrets management)

# Optional: Database migrations (if needed)
# alembic>=1.13.1