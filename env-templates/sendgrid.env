# =============================================================================
# SENDGRID SMTP CONFIGURATION TEMPLATE
# Copy these settings to your .env file and update with your values
# =============================================================================

# Email Provider Configuration
AUTH_EMAIL_PROVIDER=smtp

# SendGrid SMTP Settings
AUTH_SMTP_HOST=smtp.sendgrid.net
AUTH_SMTP_PORT=587
AUTH_SMTP_USERNAME=apikey  # Literally "apikey" - don't change this
AUTH_SMTP_PASSWORD=SG.your-sendgrid-api-key-here
AUTH_SMTP_USE_TLS=true

# Email Branding (must be verified in SendGrid)
AUTH_FROM_EMAIL=noreply@yourcompany.com
AUTH_SUPPORT_EMAIL=support@yourcompany.com
AUTH_APP_NAME=Car Wash Service
AUTH_APP_URL=https://yourcompany.com

# Optional: Email Settings
AUTH_EMAIL_VERIFICATION_EXPIRE_HOURS=24
AUTH_EMAIL_VERIFICATION_RATE_LIMIT_MINUTES=5
AUTH_PASSWORD_RESET_EXPIRE_HOURS=2
AUTH_PASSWORD_RESET_RATE_LIMIT_HOURS=1

# =============================================================================
# INSTRUCTIONS:
# 1. Sign up for SendGrid account at https://sendgrid.com
# 2. Create an API Key with Mail Send permissions
# 3. Verify your sender email/domain in SendGrid console
# 4. Replace 'SG.your-sendgrid-api-key-here' with your actual API key
# 5. Replace email addresses with your verified domains
# 6. Update AUTH_APP_NAME and AUTH_APP_URL for your application
# =============================================================================